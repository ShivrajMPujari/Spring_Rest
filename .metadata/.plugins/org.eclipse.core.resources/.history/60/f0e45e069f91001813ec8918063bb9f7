package com.bridgeIt.user.dao;

import javax.sql.DataSource;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.dao.DataAccessException;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Component;

import com.bridgeIt.user.model.User;
@Component
public class UserDao {

	@Autowired
	private DataSource dataSource;
	
	private JdbcTemplate template;
	
	public DataSource getDataSource() {
		return dataSource;
	}
	
	public int insert() {
		
		Object [] args = {"shiv@gmail.com","shiv","pass","9846732828","mumbai","dev"};
		template = new JdbcTemplate(dataSource);
		System.out.println(dataSource);
		int out=0;
		try {
			out = template.update("insert into UserLogin values (?,?,?,?,?,?)", args);
			System.out.println("number rows affected "+out);
		
		} catch (DataAccessException e) {

			System.out.println("template not excuted..");
		}
		return out;
		
	}
	

	
	
	
}
